= simple_form_for(@location) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.association :place
    = f.input :desc
    = f.input :price

  .form-actions
    = f.button :submit
