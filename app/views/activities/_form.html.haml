= simple_form_for(@activity) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.association :location
    = f.input :instruction
    = f.input :price

  .form-actions
    = f.button :submit
